import React from 'react';
import './LoadingCard.scss';
const LoadingCard = ({...props}) => {
    return (
        <div className="loading_card" style={{height: props.height}}>
            
        </div>
    )
}

export default LoadingCard
